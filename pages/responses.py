import streamlit as st
# from app import get_db_connection  # dacă ai pus conexiunea într-un fișier separat

# st.set_page_config(page_title="Răspunsuri", layout="wide")
# st.title("📋 Răspunsuri colectate")

# conn, cursor = get_db_connection()
# cursor.execute("SELECT * FROM survey_responses ORDER BY submission_timestamp DESC")
# rows = cursor.fetchall()

# if rows:
#     for row in rows:
#         st.write(row)
# else:
#     st.info("Nu există răspunsuri înregistrate încă.")

st.write("In the making...  ")
